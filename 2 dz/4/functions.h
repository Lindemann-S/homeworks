#pragma once
#include <cmath>
#include <iomanip>

using namespace std;

int gcd(int a, int b);
void quicksort(double *row, int first, int last);
struct Fraction
{
    int numerator;
    int denominator;
    double arrValue;
};
